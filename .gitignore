_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
.bundle/
vendor/
Gemfile.lock
.DS_Store

# Per-user Claude Code permissions
.claude/settings.local.json

# Image library staging — originals downloaded from Drive for selection.
# Only the chosen subset gets renamed and committed to assets/images/.
assets/images/incoming/
