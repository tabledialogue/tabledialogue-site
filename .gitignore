_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
.bundle/
vendor/
Gemfile.lock
.DS_Store

# Per-user Claude Code permissions
.claude/settings.local.json

# Image library — tracked so the proposal in docs/image-selections.md
# is reviewable on GitHub. Excluded from the Jekyll build via _config.yml
# (see "exclude:" — assets/images/incoming/ is not served).
